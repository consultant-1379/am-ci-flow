# Role for Set system config
### For run this role you could use ***jenkins_agents_configure*** playbook:
```
ansible-playbook -i inventory.yml --key-file [your_ssh_key] -u [your_username] jenkins_agents.yml
```